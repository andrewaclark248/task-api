spring:
  application:
    name: task-api
  datasource:
    url: jdbc:postgresql://localhost:5432/task_api
    username: postgres
    password: password

  jpa:
    hibernate:
      ddl-auto: update
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: https://dev-homdrgi6j31x10tc.us.auth0.com/